# Local API

Planned thin FastAPI boundary for validation, job submission, cancellation, result retrieval, and telemetry inspection.

The API must not contain physics logic and must not run heavy optimization inside the request or FastAPI background-task process.
